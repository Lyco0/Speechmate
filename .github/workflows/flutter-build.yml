name: Build Flutter APK

on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.16.9'
          channel: stable
          cache: false

      - name: Verify Flutter
        run: flutter --version

      - name: Go to Flutter project directory
        run: cd Speechmate

      - name: Clean project
        working-directory: Speechmate
        run: flutter clean

      - name: Get dependencies
        working-directory: Speechmate
        run: flutter pub get

      - name: Build release APK
        working-directory: Speechmate
        run: flutter build apk --release

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: SpeechMate-APK
          path: Speechmate/build/app/outputs/flutter-apk/app-release.apk
